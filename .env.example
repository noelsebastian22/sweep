# Sweep — environment template. Copy to .env and fill in. Never commit .env.

# ---------------------------------------------------------------------------
# Client-side. Safe to ship in the browser bundle — RLS protects the data,
# not the key.
# ---------------------------------------------------------------------------
SUPABASE_URL=https://ifwyufrepqkzsicjinfi.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_ZvLSR51CAB8-vGonA7qmHw_P_oWLAEU

# ---------------------------------------------------------------------------
# Edge function secrets. Set with `supabase secrets set KEY=value`.
# These must never appear in the client bundle, in this repo, or in a log line.
# ---------------------------------------------------------------------------
# SUPABASE_SERVICE_ROLE_KEY=
# GOOGLE_PLACES_API_KEY=          # metered — see AGENTS.md hard rule 1
# GOOGLE_PSI_API_KEY=             # free, rate-limited

# ---------------------------------------------------------------------------
# Local only.
# ---------------------------------------------------------------------------
# SUPABASE_PROJECT_REF=ifwyufrepqkzsicjinfi
